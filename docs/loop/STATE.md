# LOOP STATE（跨上下文续接的唯一权威现场，每轮收尾必须更新）

- 当前状态：**第四期 loop 已收尾**——10 轮（029–038）全部关闭，STATE 三候选 + 扫描衍生候选全部处置
- 本期成果（9 commits，1 安全 + 2 功能 + 4 个存量 bug 修复 + 2 个 CI 阻塞修复）：
  - 6635423 029：importAllData 键白名单（域外键注入防护 + Toast 显式计数）
  - b2b1411 030：快捷键面板移动端入口（首页页脚链接，双端受益）
  - ce67e51 031：SW 离线兜底（offline.html + navigate 离线分支；真停服实测两层兜底）
  - c64fb39 032：临境聊天/抽认卡 aria 标注补齐
  - 7fee65a 033：**存量 bug**——润物表勾选进度文本自诞生从未刷新（querySelector("strong") 命中 tip-banner）
  - 460e6d6 034：**存量 bug**——规则跳转永远命中第一条（jumpToRule(3) 实测到①）；data-rule 锚点
  - d785088 035：**存量 bug**——实时同步拉到收藏/场景后界面从不刷新（错误注释 + 反条件）
  - 58600b5 037：双设备 CI 两个阻塞（Bash 3.2 $BASE 中文越界 + sync.js Node 环境 setInterval 守卫）→ 6/6 PASS
  - 038：标杆对照（零偏差；基线补双设备门 + macOS Bash 3.2 变量规范）
- 036：验证轮（局部状态更新失效族全库扫描闭合：toggleMastered/setCollectStatus 均用正确 id 锚点/整页重绘模式）
- 待用户拍板事项（**优先级最高**）：**推送 main 触发生产部署**——本地领先 39 commits，生产停在 8 月构建（StatsPage.js 404）；本期 4 个存量 bug 修复全在线下
- 版本戳当前值：`?v=20260830z`
- 方法论沉淀（本期新）：
  - 「局部状态更新」族横扫法：033 抓到后 034/035 连续命中同族——单一 bug 是扫描族的信号
  - 注释与实现不符 = bug 证据（035 注释宣称的 refresh 调用不存在）
  - CDP setOffline 绕过 SW，验证离线兜底必须真停服（031）
  - 断言口径错误会误报正常态（030 视口内 / 034 block:center 末条边界）
- 下一期候选（扫描备忘）：
  - 036 验证时训练页健康，但「已掌握筛选 + 卡片隐藏」的组合在移动端 375px 未走查（低优先）
  - functions/api 的 D1 索引审查（collections 按 user_id 查询频繁，已有索引待核对覆盖度）
  - E2E 断言数从 29 → 扩充自检断言入 E2E（ci-run 与 self-check 断言有重叠，可收敛为单一来源）
