# Iteration 000 — WIP 收口：移动抽屉 + 拾遗三态 + 复习进度条

- 日期/耗时：2026-09-06 / ≤30min
- 队列来源：A0（LOOP_PLAN §5 主线 A 第 0 项）

## Step1 问题与反问

【问题 1】工作区存在一批未提交改动（5 文件 +211/−57），按 R2「有效果才 git」需先验收其改进是否成立，否则后续迭代将在脏基线上进行。
- 现象/证据：`git status` → `M css/style.css / index.html / js/app.js / sw.js / tests/e2e/self-check.js`；改动内容为：移动端导航改为右侧抽屉（含横向溢出修复）、拾遗卡片三态按钮 + 删除钮拆分、复习模式进度条、自检新增抽屉段、版本戳 bump 至 `20260830c`。
- 反问 1（真实性）：改动是否真实可达？→ 复核：`git diff` 显示 `css/style.css` 新增 `.nav` 抽屉块（visibility 双阶段过渡）、`js/app.js` 新增 `closeMobileDrawer()` / `toggleMobileMenu()` 重写、`renderWordListCard()` 三态按钮。
- 反问 2（性价比）：这批改动命中标杆 §2（z-index 层级表、overflow-x: clip、visibility 双阶段）与 §3（抽屉四关闭途径、aria-expanded），是标杆本身的来源，验收即标杆校准。
- 反问 3（最小路径）：无需改动，仅跑门验收 + commit。
- 分论点：a) 三个静态门已提前跑过全绿（🔴0 / 资产 ✅ / 对比度 16 组），剩余缺口在 E2E、视口、自检三个动态门；b) 自检脚本已包含本轮新增的抽屉断言段，验收即同时验证防复发门本身。

## Step2 方案步骤

1. 启动本地双服务器（`bash start.sh` 或分别 `node web_server.js` / `node tts_server.js`）；
2. 依次跑 `npm run test:viewport`、`PW_CHANNEL=chrome npm run test:e2e:ci`、`npm run test:selfcheck`；
3. 全绿 → 按 R3 提交一个 commit（feat 移动端抽屉导航 + 拾遗卡片与复习进度优化）；
4. 更新 STATE.md（轮号 001，队列指针 A1）。

## Step3 验收标准

- 命令级：三个静态门 + `test:viewport` + `test:e2e:ci` + `test:selfcheck` 全绿；
- 断言级：自检抽屉段输出「开合 / 遮罩 / aria / 顶栏可点 / 无溢出 全部正常」；E2E ≥ 29 PASS 无回归；
- 反例级：若视口测试出现新增溢出截图，或 E2E 数量低于基线，本轮 FAIL，改动回炉而非 commit。

## Step4 实施

（本轮无新改动，纯验收 + 提交。）

## Step5 验收结果

- 状态：**PENDING**（待执行 Step2 第 1–2 步，结果回填此处）

## 结论：PENDING ｜ commit：未提交
