# Iteration 012 — 抽认卡发音按钮

- 日期/耗时：2026-09-06 / ~12min ｜ 结论：**PASS ｜ commit：见 git log（本记录随下一轮提交）**

## Step1 问题与反问
【问题 1】复习抽认卡只有文字，无发音入口——听音是韩语复习的核心环节，用户得复制文本去砥砺页或依赖系统 TTS。
- 反问 1 → 真实（010 取证：renderWordCard 无任何 TTS 调用）。
- 反问 2 → 命中复用范式：项目已有 `playBtn(text, size)` 组件（encodeURIComponent 传参防注入、自带 `event.stopPropagation()` 不会误触翻面、TTS 不可用时走既有报错路径），词干页/AI 示例等 5+ 处已用。零新代码路径。
- 反问 3 → 最小路径 = 卡片正面韩文旁内联 `playBtn(c.text, "small")`，仅正面（复习的听音诉求发生在看到韩文时）。

## Step2 方案步骤
renderWordCard 正面插入 playBtn ｜ 自检 3d* 断言扩展 hasSpeak ｜ 版本戳 h→i。

## Step3 验收标准
- 命令级：六门全绿。断言级：复习态 `.flashcard .korean-speak-btn` 存在。反例级：点发音按钮不得翻面（playBtn 自带 stopPropagation，既有范式保证）。

## Step5 验收结果
六门全绿 + 自检「复习键盘：→ / 空格 / ← / 发音按钮 全部正常」。E2E 29 PASS。
## 结论：PASS ｜ commit：随 013 一并提交
