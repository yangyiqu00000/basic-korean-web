# Iterations 029–031 — 导入白名单 / 移动端快捷键入口 / SW 离线兜底

- 日期：2026-09-07 ｜ 3 轮全部 PASS ｜ commits：6635423 / b2b1411 / ce67e51

## 029 — importAllData 键白名单收紧（安全项）
【问题】STATE 候选：导入按 Object.keys 回放任意键——被篡改/手改备份可注入任意 localStorage 键、塞爆配额、结构化数据被 String/stringify 双重扭曲。
- 处置：白名单 = ALL_STORAGE_KEYS + PLAIN_STORAGE_KEYS（备份合法域，复用 026 单一事实源）；域外键跳过 + Toast 显式计数（静默会掩盖导出端 bug）。
- 自检断言：恶意键（evil_injected_key/x_hax）拦截 ×2 / 域内键（korean_theme）放行。
- 结论：PASS ｜ 6635423

## 030 — 快捷键面板移动端 UI 入口
【问题】STATE 候选：? 键需实体键盘，移动端不可达；顶栏按标杆 §2 禁加第 5 按钮。
- 设计决策：落点 = 首页底部既有「⌨️ 快捷键：数字键 1-8」提示行——尾追「查看全部快捷键」链接。零新布局、双端受益、复用 toggleShortcutsHelp 单一路径。
- 断言口径纠偏（本轮自我修复）：首轮断言要求链接「在视口内」——页脚链接在首屏折叠线下属正常，判据应为「有实体 + 点击行为」。教训：**断言口径写错会把正常态误报为缺陷**（与 004 轮「缩略图误读」同族：证据判读错误）。
- 结论：PASS ｜ b2b1411

## 031 — SW 离线兜底页
【问题】STATE 候选：SW 对 HTML 永不缓存 + 无离线页 → 离线导航 = Response.error() = 浏览器恐龙错误页。
- 实施：offline.html（token 摘要内联——离线时主 CSS 可能未缓存故不能外链；暗色 media query 自适应）+ SW 两处：install 预缓存（waitUntil 内，先于 skipWaiting）；fetch 对 navigate 模式加离线分支（缓存优先 → offline.html → Response.error）。
- 实测方法学（CDP 断网绕过 SW 的坑）：Playwright setOffline 在网络栈层拦截、绕过 SW fetch——**验证 SW 兜底必须真停服**。结果：杀服后 SPA 导航无感（① 句的助记正常渲染）；整页 reload 拿到完整首页（navigate 分支 caches.match 命中整包缓存）——两层兜底均成立，offline.html 仅在从未缓存时出现（正确降序）。
- 结论：PASS ｜ ce67e51

> 三轮六门全绿（E2E 29 PASS）。生产影响：均未推送（累计 33 commits 待拍板）。
