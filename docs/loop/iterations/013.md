# Iteration 013 — 弹窗焦点陷阱 + 焦点归还

- 日期/耗时：2026-09-06 / ~18min ｜ 结论：**PASS ｜ commit：2c254ad**

## Step1 问题与反问
【问题 1】统计/快捷键/确认框三类弹窗均无焦点陷阱——键盘用户 Tab 会逃逸到被遮罩挡住的背景内容；关闭后焦点丢失到 body，屏幕阅读器/键盘用户「找不到自己」。
- 反问 1 → 010 取证真实（全库无 trap/focus 归还代码）；bkConfirm 已有「默认聚焦取消」防误触但无循环无归还。
- 反问 2 → a11y 标准实践（aria-modal 配套焦点管理）；风险低——只在既有 ESC 处理器加 Tab 分支。
- 反问 3 → 最小路径：共享 focusModal/restoreModalFocus 助手（记住打开者→归还），Tab 循环集中在 initBkConfirmKey 一个监听器。

## Step2 方案步骤
initBkConfirmKey 加 Tab/Shift+Tab 分支（三弹窗按 shortcuts→stats→bkConfirm 优先级取其一）｜ 三弹窗接线 focusModal/restoreModalFocus ｜ 自检真实键盘断言。

## Step3 验收标准
- 命令级：六门全绿。断言级（已入自检，用 Playwright 原生 keyboard 非 dispatchEvent）：打开后焦点在弹窗内；Tab×5 后仍在弹窗内；Esc 后焦点回到 .stats-toggle。反例级：无弹窗时 Tab 行为不变（分支提前 return）。

## Step5 验收结果
六门全绿 + 自检「焦点陷阱：移入 / Tab 循环 / Esc 归还 全部正常」；E2E 29 PASS。
## 结论：PASS ｜ commit：2c254ad
