# Iteration 008 — B6：快捷键帮助面板 + 存量 bug「按 0 开统计」从未绑定

- 日期/耗时：2026-09-06 / ~30min
- 队列来源：B#6 ｜ 结论：**PASS ｜ commit：6926057**

## Step1 问题与反问

【问题 1】快捷键 0–8 只存在于 hover title，无任何可发现入口。
- 反问 1 → 证据：KEY_PAGE_MAP 仅 1–9（js/app.js init 内）；顶栏按标杆 §2 约束不能加第 5 个按钮 → `?` 键呼出是唯一正确入口。真实。
【问题 2】（实施中发现）index.html:51 tooltip 宣称「统计 (按 0)」，但 **0 键从未绑定过**——文档撒谎型存量 bug。
- 反问 2 → grep 全文无 0 键处理；E2E/自检从未覆盖此声明 → 补绑定并把断言入自检防复发。
【问题 3】（自检抓出）0 键接入后统计渲染整链崩溃：`Cannot read properties of null (reading 'forEach')` @ calcLocalStreak:239 ← renderStatsContent:182。
- 反问 3 → 根因链：自检 nudge 检查的恢复代码 `setItem(key, null)` 存入字符串 "null" → `safeParse` 合法解析出 null → `getCollections()` 无 Array 防御 → `.forEach` 崩。修两层：`getCollections` 加 Array 防御（与 getWordListReviewLog 同标准）+ 自检恢复改 null 安全（removeItem）。顺手把自检 pageerror 采集升级为记录堆栈（本次定位即受益）。

## Step2 方案步骤
1. `toggleShortcutsHelp()` 全局函数（内联 onclick 只解析 window 名字）+ 复用 stats-overlay/modal/row 零新 CSS；2. init 键盘处理器补 `?` 与 `0→openStats`；3. ESC 收敛进 initBkConfirmKey；4. getCollections 防御 + 自检堆栈采集 + 快捷键断言；5. 版本戳 e→f→g。

## Step3 验收标准
- 命令级：六门全绿。
- 断言级（已入自检）：? 呼出面板且含统计行 / Esc 关闭 / 按 0 真的打开统计仪表盘。
- 反例级：输入框内按 ?/0 不得触发（INPUT/TEXTAREA 守卫已有）。

## Step5 验收结果
六门全绿 + 自检「快捷键面板：? 呼出 / Esc 关闭 / 0 开统计 全部正常」+ 全局异常清零。
## 结论：PASS ｜ commit：6926057
