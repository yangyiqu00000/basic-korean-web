# Iteration 041 — 训练卡收藏★右上角重排（Browser Use 生产实测反馈驱动）

- 日期：2026-09-07 ｜ 结论：**PASS ｜ commit：ad04692（已推送部署 20260907a）**

## Step1 问题与反问
【问题 1】用户在生产站实测时指出：抽丝训练卡顶部行的收藏★按钮位置不好——DOM 取证确认 ★ 悬在卡片水平中央（x=614/1160 宽卡），挤在「#1 · 自我介绍」编号与行尾「标记掌握」按钮之间，视觉割裂。
- 反问 1（真实性）→ 生产 DOM 测量实锤：star x=614（卡片中心 620 附近），static 行内，与编号区右缘（x=143）相隔 470px 空白。真实。
- 反问 2（范围）→ .collect-btn 有 6 处挂载（训练卡/筑基表格 td×2/词干行/AI 行/临境消息行）。表格与行内场景位置合理（紧贴内容），**只有训练卡需要重排** → 作用域限定 `.sentence-top .collect-btn`，全局样式不动。
- 反问 3（最小路径）→ 与拾遗卡 `.wl-delete` 已验证的范式对齐：卡片 `position:relative` + 钮 absolute 右上角 14px；master-btn 加 margin-right 让位防重叠。

## Step2 方案步骤
1. `.sentence-card` 加 position:relative（定位上下文）；2. `.sentence-top .collect-btn` 改 absolute top/right 14px；3. `.sentence-top .master-btn` margin-right:40px 让位；4. 版本戳 20260907a；5. 六门 + 双端（1280/375）本地实测。

## Step3 验收标准
- 命令级：六门全绿。断言级：★ absolute 且 距卡右缘≈15px / 上缘<22px / 与 master 钮无重叠（1280 与 375 双端）；生产复验同断言。
- 反例级：筑基表格/AI 行内 collect-btn 必须保持行内（作用域外不动）。

## Step5 验收结果
- 六门全绿（E2E 29 PASS；中途 E2E 异常为孤儿浏览器进程干扰，清理后恢复）。
- 本地 1280：starXY[1174,643] absolute、距右缘 15px、距上缘 15px、noOverlap=true ｜ 375：star 在卡内右侧 x=309/335、noOverlap=true。
- 生产（部署 20260907a 后）：starInCorner=true、starPos=absolute、starXY[1167,643] 距缘 15px。
## 结论：PASS ｜ commit：ad04692（已部署生产）
