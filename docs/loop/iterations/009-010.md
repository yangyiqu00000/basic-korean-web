# Iteration 009/010 — C3 生产走查 + 新队列重扫描（验证/扫描双轮，无代码改动）

- 日期/耗时：2026-09-06 ｜ 结论：009 **发现生产部署过期（无本地可修代码，不 commit）**；010 新候选入队

## Iteration 009 — C3 生产自检 + 生产视口

【发现】生产（basic-korean.pages.dev）**停在 ~8 月 6 日时代的旧构建**：
- 自检 20 项 findings 全部同根：部署版 index.html 引用已删除的 `js/components/StatsPage.js?v=20260806a` → 404 + MIME 拒绝；抽屉/统计仪表盘/拾遗统计/首页提醒/快捷键等 8 月下旬特性全部不存在（版本偏斜产物，非本地回归）；
- 生产视口 4 断点全 PASS（旧布局本身没坏）；
- **修复动作 = 部署当前 main**：本地领先 10 个 commit（含 Phase 4 后全部修复与两个新特性），E2E 29 PASS 全绿。推送 main 会触发 CI 生产部署——对外动作，**已上报待用户拍板，loop 不擅自推送**。
- 用法备注：`test:selfcheck:prod` npm 脚本未带 PW_CHANNEL，本地须 `PW_CHANNEL=chrome E2E_BASE=… npm run test:selfcheck:prod` 手动组合（已记 STATE）。

## Iteration 010 — 队列重扫描（四个假设一次性取证）

| 假设 | 证据 | 处置 |
|---|---|---|
| a) 抽认卡无键盘支持 | flip/next/prev 仅 onclick（app.js:641-653），复习是高频核心操作 | **采纳 → 011** |
| b) 抽认卡无发音入口 | renderWordCard 无任何 TTS 调用；项目已有 `playBtn()` 现成组件 | **采纳 → 012** |
| c) iOS safe-area 缺失 | `@supports (padding-top: env(safe-area-inset-top))` 已存在（style.css:1422） | 证伪 |
| d) 弹窗无焦点陷阱 | stats/shortcuts/bkConfirm 均无 trap，Tab 逃逸到背景 | **采纳 → 013** |
| C1 复习日志跨设备 | AGENTS 指定路径：D1 加表 + 迁移 | **采纳 → 014-016（仅本地 D1 验证，不动生产）** |

## 结论：两轮均无代码改动 ｜ commit：无（记录随 011 提交）
