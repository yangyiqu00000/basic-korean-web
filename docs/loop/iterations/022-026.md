# Iterations 022–026 — 竞态清零 / 清空补漏 / PWA 对齐 / 备份补漏 / 键清单根治

- 日期：2026-09-07 ｜ 5 轮全部 PASS ｜ commits：e1a2879 / c289865 / 4596696 / 503c91b / c83f0f8

## 022 — 021 同款兜底竞态全库清零
【问题】021 修复 rerenderWordList 后，`grep "root || document"` 发现 switchSkeletonTab 与 setTrainingFilter 是一模一样的「root 为 null 时兜底写 #mainContent」模式——同样的竞态窗内同样会抹掉 #vue-root。
- 处置：两处同款修复（root 缺失 → refreshCurrentPage()），全库模式清零。E2E「筛选后 #vue-root 保留」断言双重护航。
- 结论：PASS ｜ e1a2879

## 023 — clearData(ALL) 漏复习日志键
【问题】014 起复习日志已是同步资产，但 clearData("ALL") 键清单没加它——清空所有学习数据后复习统计残留，用户无从清除。
- 反问（同族扫描意识）：set 型清空语义核对——SYNC_TYPES !== "map" 分支自动上推空数组，云端整包覆盖，另一端并集后归零，语义正确。
- 自检断言：ALL 清空后 日志/收藏 localStorage 键为 null + getWordListReviewStats().total === 0。
- 结论：PASS ｜ c289865

## 024 — PWA 主题色对齐设计系统
【问题】manifest theme_color 仍是 #c46a29——被对比度整改淘汰的旧主色（3.86:1，CSS token 注释自证）；且 index.html 缺 theme-color meta，移动端地址栏颜色不受控、无暗色适配。
- 处置：manifest → #af5e25；meta 双值（light #af5e25 / dark #1a1714，media query 自动切换）。
- 结论：PASS ｜ 4596696

## 025 — exportAllData 同族漏键
【问题】023 修了清空侧后按同族意识查导出侧——exportAllData 键清单同样漏 korean_wordlist_review_log，「备份数据」导不出复习历史。
- 处置：补键；导入路径按 Object.keys(data) 通用回放 + syncPut 优先，导出补键后天然闭环。
- 结论：PASS ｜ 503c91b

## 026 — 键清单单一事实源（根治轮）
【问题】两轮各漏一次同一键 = 系统性根因：键清单在 export/clear 两处散落，改键必须人肉同步。
- 处置：提炼 `ALL_STORAGE_KEYS`（js/app.js 顶部）单一事实源，两处引用；自检新增文本级断言（两函数源码须引用单一事实源 + ALL_STORAGE_KEYS 须覆盖全部 SYNC_BLOB_MAP 同步键）——「下一个键再漏」被门拦截。
- 结论：PASS ｜ c83f0f8

> 五轮六门全绿（E2E 29 PASS / 自检新增 3 组断言全过）。生产影响：均未推送（累计 28 commits 待拍板）。
