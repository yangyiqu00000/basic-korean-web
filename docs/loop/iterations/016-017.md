# Iteration 016/017 — 文档纠偏扫尾 + 统计复习维度补全

- 日期/耗时：2026-09-06 ｜ 结论：**双轮 PASS ｜ commits：6562528（docs）/ 5829444（feat）**

## Iteration 016 — 坑点文档纠偏（AGENTS.md / v2-upgrade-plan.md）
【问题】014 的实施推翻了 AGENTS「复习日志只存本地，同步需建表+迁移」的机制描述——user_blobs 实为通用键值表，闸门是两端白名单。坑点文档失真比没有文档更危险（会把下一个 agent 引向错误方案）。
- 处置：AGENTS.md 两处 + 设计文档 §Phase 4.3 全部改为实际机制（白名单双端 + set 型语义 + arr 折叠陷阱 + 回归脚本入口）。
## 结论：PASS ｜ commit：6562528

## Iteration 017 — 统计仪表盘补复习维度（补位轮）
【问题 1】复习日志已是同步资产，但全局统计弹窗四卡无任何复习维度——用户无法在仪表盘看到累计复习量；且云端 /api/stats 也未聚合该 blob。
- 反问 1 → 真实：renderStatsContent 四卡 = 连续学习/本周收藏/抽丝/润物，无复习。反问 2 → 复用度 100%：statCard 现成、云端 unwrapBlobData 模式现成、云回填走既有 set(id,val) 局部更新（守「不整块重渲染」坑点）。反问 3 → 最小路径 = 第五卡 + reviews 字段 + 一行回填。
- 分论点：a) 云端保全量条数（不裁 90 天），本地卡即时值走本地统计——两者口径差（本地窗口 vs 云端全量）在登录回填时以云端为准，属「云端聚合替换本地估算」既有模式（同 statDays）；b) 五卡布局经 375/320px 实测无溢出（stat-card 弹性栅格）。
- **随行门更新（E2E 拦截后同步）**：ci-run.js「4 卡片」断言 → 5 卡。这类「特性数字断言」与自检双写（self-check 同步 4→5 + hasReviewCard），防只改一边。
## Step5 验收
六门全绿（E2E 恢复 29 PASS）+ 自检「统计弹窗」段含五卡断言通过 + 320px 无溢出实测。
## 结论：PASS ｜ commit：5829444
