# Iterations 032–035 — 无障碍补齐 + 三个「局部状态更新失效」存量 bug

- 日期：2026-09-07 ｜ 4 轮全部 PASS ｜ commits：c64fb39 / 7fee65a / 460e6d6 / d785088

## 032 — 临境聊天与抽认卡无障碍标注
【问题】aria 扫描：全应用仅 6 处 aria-label，chatInput/sendChatBtn 无标注（placeholder 不可替代屏幕阅读器）、翻面卡无 role。
- 处置：chatInput + 发送按钮 aria-label；翻面卡 role=button + tabindex=0 + Enter 触发。移动端复习卡按钮 375px 布局实测健康（无溢出）。
- 结论：PASS ｜ c64fb39

## 033 — 润物表勾选进度文本从未刷新（存量 bug #1）
【发现路径】033 扫描轮窗口实测：勾选后 localStorage 已写 `{"1-0":true}` 但 `0 / 42 (0%)` 纹丝不动。
【根因】toggleCheck 的文本更新走 `querySelector("strong").nextSibling.textContent`——命中的 strong 是 tip-banner 的「⚡ 核心原则」，nextSibling 是 `<br>`，赋值静默无效。**文本计数自 toggleCheck 诞生起从未生效过**，内联进度条一直正常反而掩盖了它。
【修复】renderSchedule 输出 `#scheduleProgressText` 稳定锚点；toggleCheck 精确刷新。实测 0→1/42(2%)→取消复原 0/42 闭环。自检断言：勾选前后文本变化 + 取消复原。
- 结论：PASS ｜ 7fee65a

## 034 — 规则跳转永远命中第一条（存量 bug #2，同族扫描产出）
【发现路径】034 扫描「结构脆弱选择器」族：jumpToRule(3) 实测滚到规则①。
【根因】`querySelector(".rule-header")` 永远拿第一条——从 AI 结果点「规则③/⑤/⑦」全部定位到①。用户未察觉是因为「至少滚了」。
【修复】rule-header 渲染加 `data-rule="${idx+1}"` 锚点；跳转 `querySelector('.rule-header[data-rule="n"]')`。实测 ①/④/⑦ 全部精确锚定。
【口径沉淀】block:'center' 对末条规则受页面高度限制不可达中心——断言口径应为「目标锚点可见于视口」（block:center 语义边界）。
- 结论：PASS ｜ 460e6d6

## 035 — 实时同步拉到收藏/场景后界面从不刷新（存量 bug #3）
【发现路径】035 扫描「同步层与 UI 数据流死角」。
【根因】双重错误：① doRealtimePull 注释宣称「mergeCollections/ScenesFromServer 内部已调 refreshCurrentPage」——**实测两个函数内都没有**（注释撒谎）；② `if (!hasRecords)` 反条件使刷新恰恰在「有收藏/场景更新」时被跳过——跨设备收藏到达后界面不显示，直到用户手动切页。
【修复】合并完成后统一 `refreshCurrentPage()`（Vue 走 pageTick 重建，安全；注释同步改真）。
- 结论：PASS ｜ d785088

> 四轮六门全绿（E2E 29 PASS；自检累计新增断言：润物表刷新 / 规则跳转锚定）。生产影响：均未推送（累计 38 commits 待拍板）。
